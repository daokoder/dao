
const N = 10;

class Producer
{
	routine Run( chan : mt::channel<int> ){
	}
}

class Consumer
{
	routine Run( chan : mt::channel<int> ){
		io.writeln( $finished )
	}
}

chan = mt::channel<int>(100)

producer = Producer() !!
consumer = Consumer() !!

producer.Run( chan )
consumer.Run( chan )
